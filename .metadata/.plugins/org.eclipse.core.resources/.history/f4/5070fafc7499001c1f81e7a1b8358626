package collation;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.ArrayList;

public class Example3_Hashset {
public static void main(String[] args) {
	ArrayList al = new ArrayList();
	al.add("100");//inseration order is randomaly
	al.add("Hari");//not duplicate element are allowed
	al.add('H');//all types of data stored
	al.add(34.3f);//storage type is hashtable means first generate address then generate data
	al.add('H');//null value allow only one except treeset because treeset not allowed value 
	al.add(null);
	al.add(null);
	HashSet hs = new HashSet(al);
	System.out.println(hs);
	ArrayList al2 = new ArrayList(hs);
	System.out.println(a12);
}
}
